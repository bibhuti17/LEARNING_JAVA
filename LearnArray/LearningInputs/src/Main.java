import java.io.IOException;

//TIP To <b>Run</b> code, press <shortcut actionId="Run"/> or
// click the <icon src="AllIcons.Actions.Execute"/> icon in the gutter.
public class Main {
    public static void main(String[] args) throws IOException {
        /*
         * Scanner class breaks input into tokens using whitespaces as default delimiter (we can change it to comma, slashes, etc
         * 1 - import
         * 2 - create object and initialize the constructor
         *
         *
         * Buffered Reader read form text file
         * good performance read larger text
         * buffered reader reads character
         * but buffered input stream reads raw byte
         * internally uses buffer
         * java.io package
         *
         * if has two constructors - Br(Reader in) and br(Reader in, int sz - buffer size)
         * br = new
         * Stirng line = br.readLine();
         * br.close();
         *
         *
         * Why we need fast I/O ?
         * */

//        System.out.println("Enter a number : ");
//        int num = System.in.read(); //gives ascii value
//        System.out.println(num - 48);

    }
}